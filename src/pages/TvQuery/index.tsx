import QueryPageSection from '@/sections/QueryPageSection';
import React, { PropsWithChildren } from 'react';

const TvQuery: React.FC<PropsWithChildren> = () => {
  return <QueryPageSection type='tv' />;
};

export default TvQuery;
